// Product Model (Blue Print)
class Product {
  constructor(author, title, desc, url,date,curl) {
    this.author = author;
    this.title = title;
    this.desc = desc;
    this.url = url;
    this.date = date;
    this.curl=curl;
  }
}
export default Product;
