// Product Model (Blue Print)
class Product {
  constructor(author, title, desc, url,date) {
    this.author = author;
    this.title = title;
    this.desc = desc;
    this.url = url;
    this.date = date;
  }
}
export default Product;
